version: 2

models:
  - name: dim_customers
    description: One row per customer.

    columns:
      - name: customer_key
        data_tests:
          - not_null
          - unique

      - name: customer_id
        data_tests:
          - not_null
          - unique